/**
 * telegram-bot.js — STUB (structural twin)
 * Intended function: Mobile quick-access bot with single-operator chat-ID allow list.
 * Original pattern:  Quick queries plus draft review loop; all output passes sanitizeOutput() before delivery.
 * Secrets:           fetched at runtime from Key Vault via managed identity
 *                    (see /opt/twin-bootstrap/fetch-secret.sh) — never from disk.
 * Status:            NOT IMPLEMENTED. Logs invocation and exits.
 */
console.error('[stub] telegram-bot.js invoked — not implemented');
process.exit(0);
