/**
 * send-email.js — STUB (structural twin)
 * Intended function: Outbound email for OPERATOR NOTIFICATIONS ONLY.
 * Original pattern:  Hard rule: never sends to external stakeholders — drafts are sent manually by the operator.
 * Secrets:           fetched at runtime from Key Vault via managed identity
 *                    (see /opt/twin-bootstrap/fetch-secret.sh) — never from disk.
 * Status:            NOT IMPLEMENTED. Logs invocation and exits.
 */
console.error('[stub] send-email.js invoked — not implemented');
process.exit(0);
